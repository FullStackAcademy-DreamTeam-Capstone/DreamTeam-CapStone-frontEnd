import React, { useEffect } from "react";
import { createCartItem } from "../apiAdapter";

const AddToCart = (props) => {
    const [price, setPrice] = useEffect('');
    const [quantity, setQuantity] = useEffect('')
}

export default AddToCart;